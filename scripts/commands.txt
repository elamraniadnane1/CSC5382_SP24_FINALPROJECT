zenml connect --url http://localhost:8080

pip install "zenml[server]==0.56.1"

pip check zenml[server]

docker exec -it wonderful_sl cqlsh
DROP KEYSPACE "keyspace";
CREATE KEYSPACE "keyspace"
WITH replication = {'class': 'SimpleStrategy', 'replication_factor' : 1};
DESCRIBE keyspaces;

CREATE TABLE keyspace.features (
    id uuid PRIMARY KEY,
    tokens list<int>,
    label int
);


mlflow ui

dino-admin.ddns.net

zenml integration install feast

zenml stack register custom_stack -e mlflow_experiment_tracker ... --set
zenml experiment-tracker register mlflow_experiment_tracker --flavor=mlflow

docker run -p 9042:9042 cassandra
zenml 0.56.2 requires click<8.1.4,>=8.0.1, but you have click 8.1.7 which is incompatible.
Successfully installed click-8.1.7

Install ZenML

pip install "zenml[templates]==0.56.2"

Login to your ZenML Cloud tenant

zenml connect --url https://1947a1bf-zenml.cloudinfra.zenml.io
zenml connect --url=https://1947a1bf-zenml.cloudinfra.zenml.io

cqlsh> DROP KEYSPACE "keyspace_1";
cqlsh> CREATE KEYSPACE "keyspace_1" WITH replication = {'class': 'SimpleStrategy', 'replication_factor' : 1};
cqlsh> CREATE TABLE "keyspace".features (
   ...     id UUID PRIMARY KEY,
   ...     tokens list<int>,
   ...     label text
   ... );


CREATE TABLE "keyspace".features (
    id UUID PRIMARY KEY,
    tokens list<int>,
    label text
);



curl -X POST http://localhost:8000/predict -H "Content-Type: application/json" -d "{\"text\":\"We love Biden\"}"

docker logs test-fastapi

docker run -p 8080:8000 --name test-fastapi fastapi-model-server


# Build the Docker image
docker build -t fastapi-bert .

# Run the Docker container
docker run -d -p 8000:8000 fastapi-bert


